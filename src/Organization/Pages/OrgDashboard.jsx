import React from 'react';
import Layout from '../../Layout';
import { setUser } from '../../store/loginSlice';
import { useDispatch } from 'react-redux';

export const OrgDashboard = () => {
  
 
  return (
    <Layout>
      <h1 className='text-center'>Org Dashboard</h1>
    </Layout>
  );
};

